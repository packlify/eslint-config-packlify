# eslint-config-packlify

```bash
bun add eslint-config-packlify -d
```

`.eslintrc`

```json
{
  "extends": "eslint-config-packlify"
}
```

or

```javascript
// .eslintrc.js

export default = {
  extends: 'eslint-config-packlify',
};
```
